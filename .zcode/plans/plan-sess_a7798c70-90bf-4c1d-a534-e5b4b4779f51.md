# Simplify Cooling-with-heat → DL (PINN + RL) focus, with an API-fed data layer

## Target shape
Two code dirs (matches your ≤2-subfolder rule), everything else gone or archived:
- **`harness/`** — physics + gym envs + backends (grad / search / **RL-PPO**) for heat-driven cooling (Cycle0D, Bed1D, TwoBed, AbsorptionCycle). The RL/control surface.
- **`adsorbent-ml/`** — API-fed data pipeline + DL models (bed PINN, tabular baseline) + training + eval.
- **`attic/`** (tracked, outside packaging/tests/linting) — parked GUI + legacy docs.
- **`diffheat/`, `Materials/`, PDE zoo, `examples/`** — deleted (git history preserves) or moved out of the repo.

## Phase 0 — Safety checkpoint (commit first)
The entire DL codebase (adsorbent-ml models/training/eval/features, bed PINN, second-gen harness work, GUI, tests) is currently **uncommitted**. Commit it as-is in ~3 thematic commits (harness trials+gui+h3; adsorbent-ml pipeline; tests+config) before any deletion.

## Phase 1 — Delete the PDE zoo + diffheat
- Delete `diffheat/`, `examples/`, `docs/superpowers/`, top-level `tests/test_*.py` (diffheat's ~320 tests).
- Delete harness trial envs + physics twins: `envs/{heat1d,heat2d,heat3d,cloak2d,natural_conv,forced_conv,boussinesq,thermoelectric,te_1d,telegrapher,adapter}.py`, `physics/{heat1d,heat2d,heat3d,cloak2d,natural_conv,forced_conv,boussinesq,thermoelectric,te_1d,telegrapher,adapters}.py`, plus `harness/{mesh,operators,solvers,experiments}/`.
- Delete their tests (`test_*_trial.py` except `test_absorption_trial.py` — absorption IS heat-driven cooling, env stays; `test_diffheat_trials.py`, `test_dims.py`, `test_h3_adapter.py`, `test_unstructured_contract.py`, heat-parity tests).
- Update `harness/{__init__,registry,envs/__init__,physics/__init__}.py` (drop `models`/SimulatorAdapter registry), `tests/harness/test_import_hygiene.py`, import-linter contracts in root pyproject.
- Root cruft: `git rm` June notebooks + tracked `.ipynb_checkpoints/`, `.virtual_documents/`; remove `*.egg-info/`; move stray `5faa...pde3d.pdf` to the archive.

## Phase 2 — Attic the GUI
- `git mv harness/gui attic/gui`, `git mv tests/harness_gui attic/gui_tests` (excluded automatically: pytest `testpaths=["tests"]`, import-linter `root_packages=["harness"]`).
- Remove `harness-gui` console script + `gui` extra from `harness/pyproject.toml`; note restoration path in `attic/README.md`.

## Phase 3 — Extract + archive Materials/
- Vendor `Materials/cooling_physics.py` → `tests/harness/reference/` (V1 parity oracle; update `tests/harness/conftest.py` sys.path).
- Vendor `Materials/heat_cooling_screen.py` → `adsorbent-ml/data/mp_screen.py`; update `mp_export.py` to import it locally and read `MP_API_KEY` from env var or `adsorbent-ml/.env` (copied from `Materials/.env`, gitignored).
- `mkdir -p ~/ENTERPRISE/_archive && mv Materials ~/ENTERPRISE/_archive/Cooling-with-heat-Materials` (reversible, nothing deleted).

## Phase 4 — Root rework + docs to DL focus
- Root `pyproject.toml`: rename distribution `diffheat` → `cooling-with-heat`; drop diffheat packaging + `jax[cuda12]`; keep workspace/pytest/import-linter config; dev group gains `requests` (used by `optimade_export.py`, currently undeclared), loses pyqt6.
- Rewrite root `README.md`: project = DL (PINN surrogate + RL control) for heat-driven cooling; layout, quickstart (`harness.make(...)` + `optimize(..., backend="rl")`, data pipeline entry points), pointer to ROADMAP.
- Update `ROADMAP.md`, `harness/README.md` (env table loses 10 trial envs), `adsorbent-ml/README.md` — next DL milestones: PINN real-condition hardening, RL on stochastic TwoBed (H2.4/H3), Stage-2 GNN deferred.

## Phase 5 — Data layer: finish API pulls + organize for DL
1. **OPTIMADE bulk pull** (code already works, cache never persisted): federated discovery → pull ≥500 MOF structures from MOF-bearing providers → persist `data_cache/optimade/<db>/{structures.jsonl, manifest.json}` + CIFs.
2. **Stability tables**: script the MOFSimplify/Zenodo CSV fetch into `data_cache/stability/`; if programmatic access is blocked, document exact manual steps in ACQUISITION.md.
3. **Wire IZA pores**: `features/descriptors.py` consumes `data_cache/iza/framework_data.csv` (LCD/PLD fill + `pore_iza` flag for zeolites).
4. **`adsorbent-ml/data/datasets.py`** — thin façade with named, manifest-provenanced loaders for DL dev: `material_table()` (labels+features+CIF paths), `isotherm_fits()`, `pinn_corpus(...)`, `stability_shortlist()`, `refrigerants()`.
5. **Re-run Stage 1**: labels → match → features → baseline → cycle-COP rank eval; refresh `data_cache/n2/`, report the coverage/skill delta in ACQUISITION.md.

## Verification
- `JAX_PLATFORMS=cpu uv run pytest` — suite must be green (strictly smaller than today's 1,385).
- `lint-imports` contracts pass; `uv lock` updated.
- Grep for dangling references to deleted modules (registries, __init__ exports, docstrings where load-bearing).
- Archive dirs verified intact after moves.

## Non-goals
- No new PINN/RL algorithm work (roadmap only).
- adsorbent-ml stays "library + thin CLI" (sys.path-wired, no packaging) — that's your documented preference.
- `data_cache/` (1.9 GB) stays as-is, gitignored; Materials data is regenerable legacy, hence archive-not-delete only for the *directory* per your choice.